# Linux-specific environment variables
# Works around Ollama IPv6/IPv4 binding issue on Linux
# Usage: Copy to .env or use with docker-compose --env-file .env.linux

# Path to your documents directory (absolute path)
DOCUMENTS_PATH=/home/mansoor/Syncthing/ObsidianNotes/Notes

# Ollama Configuration - using loopback for host networking
OLLAMA_HOST=127.0.0.1:11434
EMBEDDING_MODEL=mxbai-embed-large
CHAT_MODEL=qwen3:14b

# Processing Configuration
BATCH_SIZE=10
OLLAMA_RETRIES=3

# Database Configuration - using loopback for host networking
CHROMA_HOST=127.0.0.1:8000

# Service Configuration - using loopback for host networking
PROCESSOR_HOST=127.0.0.1:8001